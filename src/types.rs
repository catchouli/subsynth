//! Shared type definitions.

/// A type representing the time input of a synth network.
pub type Time = f64;

/// A type representing the frequency input of a synth network.
pub type Frequency = f64;

/// A type representing the output of a synth network.
pub type Sample = f64;

/// A type representing a midi note.
pub type MidiNote = u8;
